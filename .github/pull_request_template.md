## What and why

<!-- What changed, and why — the problem this solves, not just a description of the diff. -->

## Checklist

- [ ] `go build ./... && go vet ./...`
- [ ] `go test ./... -race -count=1` passes
- [ ] `wms preflight` is GO (or the failures are explained below)
- [ ] Docs updated if this changes user-visible behaviour (`docs/`, `mkdocs.yml` nav)
- [ ] No secrets, API keys or personal data in the diff

## Test plan

<!-- How you verified this: commands run, screens exercised, etc. -->

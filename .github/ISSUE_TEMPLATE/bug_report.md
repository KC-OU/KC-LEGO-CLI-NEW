---
name: Bug report
about: Something in the CLI, TUI, telnet or web terminal isn't working right
labels: bug
---

**What happened**
A clear description of the bug.

**Steps to reproduce**
1. Run `wms ...`
2. ...

**Expected**
What you expected to happen instead.

**Environment**
- `wms version` output:
- OS / platform:
- Telnet, web terminal, or local CLI?

**Logs**
Relevant output (`--json` output, `wms doctor`, or `wms audit tail` are usually most useful). Redact anything private —
API keys, real names, addresses.
